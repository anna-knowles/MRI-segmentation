Both jupyter notebooks include MRI segmentation, however the HonorsThesis notebook includes a 
segmentation slice slider when the notebook has been downloaded and is running locally as well
as showing the segmentation overlay mask. The HonorsThesis2 notebook also shows the slices
broken into 3 different parts (sagittal, axial, coronal) however the dataset did not provide
a segmentation mask layer.
